{
  "$schema": "http://json-schema.org/schema#",
  "title": "JSON schema for JWT Tokens",
  "type": "object",
  "properties": {
    "access_token": {"type": "string"},
    "refresh_token": {"type": "string" }
  }
}
